import './CartWidget.css';
import Button from '@mui/material/Button';

export default function CartWidget() {
    return (
    <div>
        <button>
            <p>CARRITO</p>
        </button>
    </div>    
    )
}